[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.backends.legacy:build"

[project]
name = "eyenav"
version = "0.1.0-alpha"
description = "Intent-First Eye Navigation Platform — Hands-Free HCI via Gaze, Blinks, and Eyebrow Gestures"
license = {text = "MIT"}
authors = [{name = "EyeNav Research Team", email = "research@eyenav.ai"}]
keywords = ["eye-tracking", "gaze-estimation", "HCI", "accessibility", "computer-vision", "intent-recognition"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "Intended Audience :: Science/Research",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3.11",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
    "Topic :: Software Development :: Libraries :: Python Modules",
    "Topic :: System :: Hardware :: Universal Serial Bus (USB)",
]
requires-python = ">=3.11"
dynamic = ["dependencies"]

[tool.setuptools.dynamic]
dependencies = {file = ["requirements.txt"]}

[project.urls]
Homepage = "https://eyenav.ai"
Repository = "https://github.com/eyenav/eyenav"
Documentation = "https://docs.eyenav.ai"
Research = "https://research.eyenav.ai"

[project.scripts]
eyenav = "eyenav.cli:main"

# ─────────────────────────────
# Ruff Linter Configuration
# ─────────────────────────────
[tool.ruff]
target-version = "py311"
line-length = 100
src = ["backend", "tests"]

[tool.ruff.lint]
select = [
    "E",   # pycodestyle errors
    "W",   # pycodestyle warnings
    "F",   # pyflakes
    "I",   # isort
    "B",   # flake8-bugbear
    "C4",  # flake8-comprehensions
    "UP",  # pyupgrade
    "N",   # pep8-naming
    "S",   # flake8-bandit (security)
    "ANN", # type annotations
    "D",   # pydocstyle
]
ignore = [
    "D100", "D101", "D102", "D103", "D104", "D105", "D107", "D205", "D415",  # Docstring rules
    "ANN101", "ANN001", "ANN003", "ANN201", "ANN202", "ANN204",  # Type annotation rules
    "E501",  # Line too long
    "B017",  # Blind exception asserts
    "N806",  # Variable naming
    "F821", "F841", "F401",  # Undefined/unused variables and imports
    "E701", "E722", "E402",  # Multiple statements on one line / bare except / module import
    "C408", "S311", "B007", "B905", "UP042", "S110", "W291", "W293"
]

[tool.ruff.lint.per-file-ignores]
"tests/*" = ["S101", "ANN201"]
"benchmarks/*" = ["S101", "ANN201"]

[tool.ruff.lint.pydocstyle]
convention = "google"

# ─────────────────────────────
# MyPy Type Checking
# ─────────────────────────────
[tool.mypy]
python_version = "3.14"
strict = true
warn_return_any = true
warn_unused_configs = true
ignore_missing_imports = true
exclude = ["tests/", "benchmarks/"]
explicit_package_bases = true
disable_error_code = [
    "assignment",
    "no-untyped-def",
    "var-annotated",
    "no-any-return",
    "attr-defined",
    "type-arg",
    "name-defined",
    "operator",
    "no-untyped-call",
    "union-attr",
    "misc",
    "untyped-decorator"
]

# ─────────────────────────────
# Pytest Configuration
# ─────────────────────────────
[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["backend"]
python_files = ["test_*.py", "*_test.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]
addopts = [
    "--strict-markers",
    "--tb=short",
    "-ra",  # Show extra summary for all except passed
]
markers = [
    "safety_critical: Mark test as safety-critical (must pass 100%)",
    "slow: Tests that take > 1 second",
    "requires_camera: Tests that require a physical camera",
    "requires_gpu: Tests that require GPU",
    "integration: Integration tests",
    "benchmark: Performance benchmark tests",
]

# ─────────────────────────────
# Coverage Configuration
# ─────────────────────────────
[tool.coverage.run]
source = ["backend/eyenav"]
omit = [
    "*/tests/*",
    "*/benchmarks/*",
    "*/__init__.py",
]

[tool.coverage.report]
exclude_lines = [
    "pragma: no cover",
    "def __repr__",
    "raise NotImplementedError",
    "if __name__ == .__main__.:",
    "pass",
]

# ─────────────────────────────
# Bandit Security Config
# ─────────────────────────────
[tool.bandit]
exclude_dirs = ["tests", "benchmarks"]
skips = ["B101"]  # Skip assert warnings (acceptable in test-adjacent code)
